services:
  vector-db:
    image: qdrant/qdrant
    container_name: vector-db
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage: